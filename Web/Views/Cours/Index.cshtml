@model List<Models.Cours>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(Model => Model.First().Nom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nom)
        </td>
        <td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCours }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCours }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCours })
        </td>
    </tr>
}

</table>
